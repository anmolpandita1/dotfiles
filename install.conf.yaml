- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig: 
- create:

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
    - 
      command: ./setup_homebrew.sh
      stdout: true
      stderr: true
